<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="match_parent"
    android:layout_weight="1"
    android:background="@drawable/listview_bg"
    android:orientation="vertical"
    android:padding="7dp"
    tools:context=".FileManagerWindowFragment" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="0"
        android:background="@color/transparent"
        android:padding="5dp" >

        <ImageView
            android:id="@+id/dirImage_FileManagerWindow_ImageView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@color/transparent"
            android:contentDescription=""
            android:src="@drawable/open_v2" />

        <TextView
            android:id="@+id/dirPath_FileManagerWindow_ImageView"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:background="@color/transparent"
            android:gravity="center_vertical"
            android:paddingLeft="20dp"
            android:text="@string/app_name"
            android:textSize="20dp" />
    </LinearLayout>

    <ListView
        android:id="@+id/fileList_FileManagerWindow_ImageView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="1"
        android:background="@color/transparent"
        android:cacheColorHint="@color/transparent"
        android:divider="#00000000"
        android:dividerHeight="1dp"
        android:fadeScrollbars="false"
        android:paddingLeft="25dp"
        android:scrollbarAlwaysDrawVerticalTrack="true"
        android:scrollbarThumbVertical="@drawable/scrollbar_vertical_thumb"
        android:scrollbarTrackVertical="@drawable/scrollbar_vertical_track"
        android:scrollbars="vertical" >
    </ListView>

</LinearLayout>